#!/bin/sh

: "${BUILDPATH:?BUILDPATH not set}"

. "$HOME/env/path_functions.sh"
del_from_path PATH "$WS_BIN_PATH"

mkdir -p "$BUILDPATH"
cmake -G Ninja -Werror=author -DENABLE_CCACHE=On -DCMAKE_EXPORT_COMPILE_COMMANDS=1 ${1+"$@"} -S "$SRCPATH" -B "$BUILDPATH"
