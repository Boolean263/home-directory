#!/bin/sh

set -u

if [ -z "$1" ] || [ "$1" = "-h" ] || [ "$1" = "--help" ] ; then
    cat 1>&2 <<EOT
Disable pushing to a git remote.

Usage:
    git disable-remote REMOTE_NAME

See also 'git enable-remote'.
EOT
    exit 1
fi

set -e
git remote set-url --push "$1" "file:///dev/null"
